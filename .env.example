# PostgreSQL connection string (this project uses Neon: https://neon.tech)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=verify-full

# Any long random string — used to sign session cookies (HMAC-SHA256)
SESSION_SECRET=
